<?php
declare(strict_types=1);

/**
 * Copy this file to `config.php` for local overrides.
 * The backend will keep working with the built-in defaults if no override exists.
 */

const DB_HOST = 'localhost';
const DB_NAME = 'restaurant_builder_saas';
const DB_USER = 'your_db_user';
const DB_PASS = 'your_db_password';
const DB_CHARSET = 'utf8mb4';
